## Extra chaos DAGs (separate folder)

This folder is mounted into the Airflow container at:

- `/opt/airflow/dags/extra_dags`

The goal is to keep “non-core demo DAGs” separate from `airflow/dags/`.


